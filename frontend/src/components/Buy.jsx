/* import React, { useState } from "react"; */

export default function Buy() {
  /* const processedItems = [
    "chilled-meat",
    "flour",
    "milk",
    "pepper",
    "rice",
    "salt",
    "shrimp-meat",
    "butter",
    "cream",
    "ham",
    "smoked-fowl",
    "sugar",
    "bacon",
    "cheese",
    "crab-roe",
    "jam",
    "sausage",
  ]; */
  /*   function random(min, max) {
    min = Math.ceil(min);
    max = Math.floor(max);
    return Math.floor(Math.random() * (max - min + 1)) + min;
  } */
  /*   let randomIndex = random(0, processedItems.length - 1);
  const [objectName, setObjectName] = useState(processedItems[randomIndex]);

  const handleClick = () => {
    randomIndex = random(0, processedItems.length - 1);
    setObjectName(processedItems[randomIndex]);
  };

  return (
    <div>
      <div>
        <h1>I'll sell you this item for 22 Moras</h1>
        <img
          src={`https://api.genshin.dev/materials/cooking-ingredients/${objectName}`}
          alt={objectName}
        />
      </div>
      <h2>
        {objectName.charAt(0).toUpperCase() +
          objectName.slice(1).replaceAll("-", " ")}{" "}
        : 22 Moras
      </h2>
      <div>
        <button onClick={handleClick} type="button" className="button">
          {" "}
          Next
        </button>
      </div>
    </div>
  ); */
}
